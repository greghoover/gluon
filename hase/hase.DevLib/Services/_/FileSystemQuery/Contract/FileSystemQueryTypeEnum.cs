using ProtoBuf;

namespace hase.DevLib.Services._.FileSystemQuery.Contract

{
    //[ProtoContract]
    public enum FileSystemQueryTypeEnum
    {
        //[ProtoMember(1)]
        DirectoryExists = 1,
    }
}
